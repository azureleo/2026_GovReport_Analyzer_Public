# v7-5 지시서 — 채점기 의미완화 티어의 서술형 시트 확장 (2026-07-17)

> 기준 코드: `feat/v7-extraction-fidelity` HEAD 1aae40e(테스트 347).
> 구현: Opus(별도 worktree, type 프리픽스, push 금지, 충돌 시 보고).
> 검증: Claude 재채점(비용 0). **scripts/ 채점기와 테스트만 변경** —
> agents/·utils/·config·프롬프트 불변.

## 0. 배경 — 원인 분해 실측 (2026-07-17)

서술형 시트 저리콜(01 0.2857·07 0.2394·13 0.6875, luna 축)의 원인을
미매칭 골든 행 전수 토큰 대조로 분해했다(내용 커버리지 ≥0.5 기준):

| 시트 | 채점 골든 | 내용 존재·키 발산(채점 한계) | 추출 부재(추출 한계) |
|---|---:|---:|---:|
| 01_계획개요 | 35 | **27 (77%)** | 8 |
| 07_비전전략 | 71 | **52 (73%)** | 19 |
| 13_이행관리환류 | 16 | 9 (56%) | 7 |

발산 사례: "계획 수립 목적" vs "계획 수립 목적 및 배경", "비전" vs
"장기 비전", 07의 비전문구가 전략명이 아닌 비전문구 필드에 존재,
13의 "2050 서울특별시 탄소중립녹색성장위원회" vs "서울특별시
탄소중립녹색성장위원회". **02 지역여건의 의미완화 티어 도입 근거와
동일한 상황**(같은 원천의 표기 발산)이므로 같은 처방을 확장한다.
단 02와 달리 서술형은 수치 필드가 없어 값 일치 전제를 쓸 수 없다 —
임계를 보수적으로 유지하고 쌍 전수 열람을 게이트로 삼는다.

## 1. 작업 항목 (G1)

`scripts/golden_score_matching.py`의 의미완화 티어(현행 02 전용)를
01·07·13으로 확장한다:

- **시트별 토큰 소스**(02의 `_지표토큰` 패턴을 시트별 분기로 일반화)
  — (2026-07-17 정정: 최초 지시의 `내용`·`세부전략`은 실제 계약 헤더가
  아님. 실헤더 `항목값`·`설명`으로 교정 — Opus 충돌 보고 확인):
  - 01: `항목명` + `항목값`
  - 07: `전략명` + `비전문구` + `설명`
  - 13: `거버넌스기구` + `역할`
  - 02: 현행(`지표명` + `지표세부범주`) 불변.
- **서술형 전제 조건**(02의 `_의미완화전제충족`[연도·단위·값 일치]을
  대체하는 시트별 전제):
  - 01: `개요유형` 정규화 동일(빈 값이면 무조건 통과 금지 — 한쪽만
    빈 값이면 통과 허용, 양쪽 채움·상이면 배제).
  - 07: `전략수준` 동일 또는 한쪽 빈 값.
  - 13: 전제 없음(토큰 자카드만).
- **자카드 임계 ≥ 0.6**(02와 동일), 유일 최고 후보만, 동률 비매칭 —
  기존 `_의미완화매칭하기`의 소비·동률 로직 재사용.
- 값일치율 집계에서 제외(서술형은 수치 비교쌍이 없음 — 02와 동일하게
  자연 제외됨을 확인만).
- 리포트의 "의미완화 매칭 쌍" 절에 시트별로 나열(현행 형식 재사용),
  헤드라인 "텍스트 유래(의미완화 포함)"에 자연 반영. 형식·칼럼 불변.
- `golden_score_contract.py`의 `의미완화적용시트`에 01/07/13 추가.
  문자유사(2-gram) 티어는 **02 전용 유지**(확장 금지 — 서술형 문자
  유사는 오결합 위험이 큼).

## 2. 테스트

- 01: "계획 수립 목적" vs "계획 수립 목적 및 배경" 계열 매칭 형성,
  개요유형 상이 시 배제.
- 07: 전략명 빈 값+비전문구 보유 행의 매칭 형성(비전 사례), 전략수준
  상이 배제.
- 13: 접두 발산("2050 ~"↔"~") 매칭 형성.
- 자카드 0.6 미만 비매칭, 동률 비매칭.
- 02 기존 의미완화 동작 바이트 불변(기존 테스트 유지).
- 기존 347개 커밋별 통과.

## 3. 인수 게이트 (Claude 재채점, 비용 0)

1. 커밋별 클린 worktree 테스트.
2. `output/서울_v73_luna.xlsx` 재채점(v7-4 골든):
   - **신규 의미완화 쌍 전수 열람 — 서로 다른 사실의 오결합이 하나라도
     있으면 불합격**(같은 원천의 표기 발산만 허용). 01·07·13 리콜
     상승 폭과 함께 기록.
   - 02 의미완화 기존 쌍 불변, 01/07/13 외 시트 지표 완전 불변.
   - 엄격·완화 티어 결과 불변(의미완화는 잔여 풀에서만 동작).
3. 합격 시 병합, 실측 문서(v7-3 문서에 절 추가)·기준선(채점기 v7-5
   축) 갱신 — 축 변경 명기.

## 4. 범위 밖 (기록만)

- 진짜 추출 부재 34행(01 8·07 19·13 7) — 대부분 구 계획(원전하나
  줄이기, 2017~2021 종합계획 등) 절의 세부 항목으로 추정, 라우팅/
  프롬프트 라운드 후보. 이번 확장 후 잔여 미매칭으로 정확히 남는지
  재확인 후 판단.
- 골든 명명 규약 정비 — 이번 매칭 쌍 목록이 재료.
- 13 절차단계·산출물 등 토큰 소스 추가 확장 — 오결합 실측 후.
