# M3-2 지시서 — 일반화 교정: 키 폴백·시나리오 정규화·프로브 지표 (2026-07-19)

> 근거: `docs/audit/M3-1_무골든프로브_실측_20260718.md` 과적합 신호 +
> 2026-07-19 진단(강원 관리번호 전무 220/220, 경기 05 시나리오 자유
> 문구 산재, 강원 03 세부부문 154/159 빈 값 = L2 정의 문제).
> 기준 코드: `feat/v7-extraction-fidelity` HEAD(가이드 커밋 이후).
> 구현: Opus(별도 worktree, type 프리픽스, push 금지, 충돌 시 보고).
> 검증: Claude — 3지자체 재실측(캐시·산출물 기존재, 비용 0).
> 원칙: 지자체 불변([[generalization-first]]), 프롬프트 불변(캐시 보존).

## S1 — 08/09/10 키의 관리번호→사업명 폴백 (채점기·프로브 공유 계약)

강원 문서는 관리번호 체계를 쓰지 않아(A 220/220·B 168/182 빈 값)
관리번호 기반 일차키(08 `[관리번호]`, 09 `[관리번호, 연도]`, 10
`[사업명, 연도]`는 무관)가 무력하다 — 로드맵 G4 "관리번호 형식 서울
과적합"의 실측.

- `scripts/golden_score_contract.py` `키()`의 `12_대응기반강화` 폴백
  패턴(과제ID 있으면 과제ID, 없으면 영역+과제명)을 08·09에 확장:
  - 08: 관리번호가 채워지면 `[관리번호]`(현행), 비면 `[부문, 사업명]`.
  - 09: 관리번호가 채워지면 `[관리번호, 연도]`(현행), 비면
    `[사업명, 연도]`.
  - **(2026-07-20 V1 정정 — 지시서 귀책, 실측 근거)**: 09 폴백 키
    `[사업명, 연도]`가 **이차(완화)키와 완전히 동일**해 티어 분리가
    무의미하고, 중복 키(같은 사업명·연도의 복수 행) 상황에서 엄격
    first-wins와 완화 후보 선택의 짝짓기 차이만 발생시킨다. 강원 09
    실측: 매칭 수 223 불변·티어만 이동(엄격 0→112)했는데 **값합의율
    0.6933→0.4902 하락**(W3 캘리브레이션 축이 흔들림). → **09 폴백을
    철회한다**(관리번호 없으면 엄격 키 없음 = 기존 동작, 완화 티어가
    담당). 08 폴백 `[부문, 사업명]`은 이차키 `[사업명]`보다 엄격해
    정보가 추가되므로 **유지**(강원 08 엄격 0→104·L2 채움률 0→1.0
    실효 확인, 값합의율 영향 없음 — 08은 값필드 없음).
  - 정규화는 기존 `키값()`(normalize_project_id·dedup_key_text) 재사용.
- 채점기 축 변경이므로: 서울 재채점에서 **관리번호 보유 행의 매칭
  불변**(폴백은 빈 값에서만 발동)을 게이트로 확인. 이차(완화)키 불변.
- 프로브는 같은 계약을 임포트하므로 자동 수혜(코드 변경 불필요 확인).

## S2 — 05 시나리오의 결정론 정규화 (organizer)

flash-lite가 시나리오 필드에 자유 문구("총배출량 전망", "BAU 대비
31.2% 감축", "관리권한 배출량 전망")를 넣어 05 키가 산재한다. 05
스키마 enum은 `BAU|정책반영|추가조치`(추출 프롬프트).

- `_clean_emissions_forecast`에 정규화 사전 추가(`_ACHIEVEMENT_MAP`
  패턴): BAU·전망·현행추세 계열 문구 → `BAU`, 감축·목표·정책 계열
  문구 → `정책반영`, 추가조치 계열 → `추가조치`. **원문 문구는 내부
  보존이 아니라 `전망방법원문`이 비어 있을 때 그 필드로 이관**(기존
  값 있으면 덮지 않음 — 정보 무손실).
- 매핑 불가 문구는 원값 유지(추측 금지). 긴 키워드 우선 매칭(v5
  전망방법 사전 관례). 지자체·문서 고유 명사 하드코딩 금지.
- 게이트: 3지자체 재정제(캐시 0 비용)에서 05 행 삭제 0, 서울 골든
  05 리콜·값일치 무하락, 경기 05 A/B 키합의율 상승 방향.

## S3 — 프로브 L2 채움률의 옵셔널 필드 제외

강원 03 채움률 3.1%는 결함이 아니라 지표 정의 문제다: L2가 일차키
"전부 채움"을 요구하는데 세부부문은 원문에 없을 수 있는 옵셔널
성분이다(시각 병합의 `_VISUAL_OPTIONAL_KEY_FIELDS` 관례와 동일 취지).

- `scripts/cross_backend_probe.py` L2 채움률 계산에서 옵셔널
  필드(`세부부문`, `지표세부범주`)를 분모에서 제외하고, 옵셔널 필드
  채움률은 **별도 컬럼으로 병기**(정보 보존). md/json 형식 변화는 이
  컬럼 추가로 한정.

## 테스트

- S1: 관리번호 있는 행 키 불변 / 08 빈 행 폴백 키 / 12 기존 폴백
  무회귀 / **(V1) 09는 관리번호 빈 행에서 엄격 키 없음(폴백 없음)**.
- S2: 계열별 매핑, 매핑 불가 원값 유지, 전망방법원문 이관·기존 값
  보존, 긴 키워드 우선.
- S3: 옵셔널 제외 채움률·병기 컬럼.
- 기존 371개 커밋별 통과.

## 인수 게이트 (Claude, 비용 0)

1. 커밋별 클린 worktree 테스트.
2. 서울 재채점(v7-5 축): 08/09 매칭·값일치 무하락(관리번호 보유라
   폴백 미발동 = 불변 기대), 05 무하락.
3. 3지자체 재정제 산출물로 프로브 재실행: 강원 08/09 키합의율 상승,
   경기 05 키합의율 상승 방향, 강원 03 L2 채움률 정상화(옵셔널 제외
   기준), 값 오병합 0 유지.
4. 합격 시 병합, M3-1 실측 문서에 절 추가.

## 범위 밖 (기록만)

- 05 축(지역전체/관리권한) 구분 필드 — 06과 동일한 계약 한계, 발주
  문의서에 항목 추가(별도 커밋).
- 14 granularity 발산(사업 단위 vs 분야 집계)·경기 09 종별 차이 —
  프롬프트 라운드 후보, L4 표본 대사 결과 대기.
- 강원·경기 재추출(프롬프트 불변이므로 불필요 — 재정제·재프로브만).
