# v5 검증 수정 지시서 — 2026-07-05 검수 결과 반영

> 대상 브랜치: `feat/v5-guideline-orchestration` (미푸시 — 히스토리 재작성 허용).
> 배경: v5 8개 WP 커밋(060789f…ab0111b)에 대한 명세 대조 검수에서 치명 1건,
> 중요 10건, 사소 다수가 확인됐다. 이 문서는 그 수정 지시이며, 대화 맥락 없이
> 코드베이스와 `docs/specs/v5_구현명세서.md`만으로 수행 가능해야 한다.
> 모호하면 보수적(동작 불변) 선택.

---

## 절대 제약 (v5 명세서 §1과 동일 — 재확인)

1. opt-in 플래그 기본값 변경 금지: `EXTRACTION_SHEET_CLUSTERING`(False),
   `ROUTE_DROP_UBIQUITOUS_STRONG`(False), `HYBRID_REVIEW_ENABLED`(False),
   `SHEET_CLOSED_LOOP_ENABLED`(False). 라우팅 점수/선택 로직 수정 금지.
2. OrganizerAgent는 결정론적 순수 Python(LLM 호출 금지).
3. 엑셀 17시트 계약: 기존 시트명·컬럼 이름/순서 불변.
4. 코드·주석·로그 한국어, 새 외부 의존성 금지.
5. 기존 테스트 전부 통과 + 수정 항목별 회귀 테스트.
6. 기존 추출 프롬프트 문면 변경 금지(이번 수정에 프롬프트 변경은 없다 —
   F3의 검수 대상 "선정" 로직은 프롬프트가 아니다).

## 커밋 전략

- **F2(커밋 재구성)는 rebase로, 나머지는 해당 WP 커밋에 `fixup!` 후
  `git rebase -i --autosquash`로 흡수**해 최종적으로 "WP당 1커밋, 총 8커밋"
  형태를 유지한다. 브랜치는 미푸시 상태이므로 히스토리 재작성이 안전하다.
- **완료 조건: 8개 커밋 각각에서 `python -m pytest tests/ -q` 전체 통과**
  (bisect 안전). 검수에서 66e0779 시점 5개 실패가 확인됐다 — F2 참조.
- push 금지. 완료 후 사용자 검토 대기.

---

## F1. [치명] WP7 내부 필드 `계획구분`이 11_재정투자계획 실컬럼과 충돌

**증상**: `organizer_agent.py:877-905` `_tag_prior_plan_rows`가
`("mitigation_projects", "annual_implementation", "quantitative_reductions",
"financial_plan")` 4개 시트의 행에 `row["계획구분"] = "기존계획"|"본계획"`을
무조건 덮어쓴다. 그런데 `config.py:233-236`의 `11_재정투자계획` 헤더에
`계획구분`이 **실존 컬럼**으로 있고 그 의미는 예산 분류
(`총계|온실가스감축대책|대응기반강화|기타` — `extractor_agent.py`의 시트 정의,
`image_agent.py:796`도 이 의미로 채움)다. excel_writer는 헤더 기반으로
`row.get(header)`를 기록하므로, 기존계획 장이 탐지된 문서에서는 11시트의
예산 분류값 전체가 파괴된 채 엑셀로 저장된다.

**수정**:
- 내부 필드명을 `계획구분출처`(또는 유사한 비충돌 이름)로 변경하고 4개 시트
  모두에 일관 적용한다. 어떤 엑셀 헤더와도 겹치지 않음을 확인할 것
  (`config.EXCEL_HEADERS` 전수 대조).
- 이 내부 필드를 읽는 모든 소비처를 grep으로 찾아 함께 갱신한다
  (WP3 타깃 검수·검증리포트·테스트 — `git grep '계획구분'`으로 전수 확인하되,
  11시트의 정당한 예산 분류 용례는 건드리지 말 것).
- **회귀 테스트**: prior_plan_pages가 탐지된 상태에서 financial_plan 행의
  `계획구분`(예산 분류값)이 organize 후에도 보존됨 + 내부 필드가
  엑셀 출력 헤더에 없음을 assert.

## F2. [중요] WP3 커밋(66e0779)이 bisect에서 깨짐 — 전방 의존

**증상**: 66e0779 시점 `hybrid_review_agent.py:414`가
`OrganizerAgent().organize_sheet(...)`를 호출하는데 `organize_sheet`는
WP8 커밋(ab0111b)에서야 추가된다. 66e0779 단독 체크아웃 시 테스트 5개 실패
(test_hybrid_consistency_wp3 1건 + test_hybrid_sheetwise 4건).
나머지 중간 커밋은 전부 통과 확인됨(51→56→61→65→70→73개).

**수정**: interactive rebase로 `OrganizerAgent.organize_sheet` 도입
(organizer_agent.py의 해당 메서드 + 필요 시 관련 헬퍼)을 WP3 커밋(66e0779)
이전 또는 WP3 커밋 자체로 옮긴다. 권장: organize_sheet 도입분을 WP3 커밋에
포함시키고, WP8 커밋에는 폐루프 실행 경로만 남긴다. 각 커밋의 논리적 단위
("WP당 1커밋")가 유지되도록 커밋 메시지도 정합 확인.

**완료 조건**: 8개 커밋 전부에서 `python -m pytest tests/ -q` 통과.

## F3. [중요] WP3-B 경고→행 역추적이 실제 경고 형식과 불일치해 무력

**증상**: `hybrid_review_agent.py:509-517` `_issue_row_index`는
`행\s*(\d+)` 정규식으로 행 번호를 뽑지만, organizer가 실제 생성하는 경고는:
- 감축률 불일치: 항목 `감축률 불일치({부문} {연도})` (organizer_agent.py:854) — 행 번호 없음
- 단위 스케일 혼재(:1175), 합계≠부분합(:1200) — 행 번호 없음
- 기준배출량≠배출현황: 영역="교차시트"(:1034) — 시트 매칭 자체 실패
- 중복 키 값 충돌: 영역="중복제거"(:1002) — 시트 매칭 실패
→ 명세 3-B 선정 기준 1이 대부분의 경고 유형에서 조용히 0건이 된다.
또한 `tests/test_hybrid_targeted_wp3.py:41`은 production이 절대 생성하지 않는
`"감축률 불일치 행 1"` 문구로 테스트해 이 결함을 은폐한다.

**수정** (텍스트 파싱 강화가 아니라 구조적 연결로):
- organizer가 행 단위 이슈를 생성할 때 이슈 dict에 **내부 필드**
  `대상시트키`(sheet_key)·`대상행번호`(1-base, 최종 정제본 기준)를 함께 기록한다.
  19_검증리포트 엑셀 헤더는 불변(내부 필드는 헤더에 없으므로 출력 안 됨).
  행 특정이 불가능한 교차시트·집계 이슈는 내부 필드 없이 그대로 둔다.
- `select_targeted_review_rows`(및 `_issue_row_index`/`_sheet_key_from_issue_area`)가
  내부 필드를 **우선** 사용하고, 없을 때만 기존 텍스트 휴리스틱으로 폴백한다.
- 행 번호는 반드시 **빈 행 제거·dedup 이후의 최종 행 인덱스**와 일치시킬 것
  (검수에서 `_clean_mitigation_projects`의 부문 불일치 리포트가
  `_filter_empty_rows` 이전 인덱스를 쓰는 어긋남도 확인됨 — organizer_agent.py:690-694.
  같은 방식으로 함께 교정).
- **테스트 교정**: 조작된 경고 문구 대신, organizer를 실제로 돌려 생성된
  검증리포트로 타깃 선정을 검증하는 통합형 단위 테스트로 교체한다
  (감축률 불일치 행 + 값충돌 행 각 1개 이상이 정확한 행으로 선정됨을 assert).

## F4. [중요] `HYBRID_REVIEW_INCLUDE_FAILED_PAGES` dead flag

**증상**: config.py:127에 정의만 있고 레포 전체 사용처 0건 — 명세 3-B
선정 경로 ③(원장 parse_fail/call_fail 배치 페이지의 인접 시트 행) 미구현.

**수정**: 명세대로 구현한다 — 플래그 True일 때, 추출 원장의
parse_fail/call_fail 배치가 속한 (시트, 페이지)에서 나온 행(출처페이지가
실패 배치 페이지와 교집합인 행)을 검수 대상에 추가. 기본 False이므로
기본 동작 불변. 단위 테스트 1개(플래그 on/off 대비).
구현이 supervisor→hybrid 간 원장 전달을 크게 흔든다고 판단되면, 대안으로
플래그를 config에서 제거하고 명세서에 "미구현(후속)" 주석을 남겨도 되나,
**둘 중 하나는 반드시** 하라(dead flag 방치 금지).

## F5. [중요] 이미지 병합 행(06/08/11)의 `visual_only` 누락

**증상**: `_is_visual_row`(organizer_agent.py:317-324)는
출처/인벤토리출처/전망방법원문/주요가정에서 "이미지"를 찾는데,
image_agent의 병합 경로 중 05(전망)만 `주요가정="이미지 p.N"` 마커를 넣고
(image_agent.py:842), 06 목표(:846-860)·08 사업목록(:775-790)·11 재정(:795-805)
경로는 마커 필드가 전혀 없다 → 이미지 유래 행이 `reported`로 저장.

**수정**: 명세 WP5가 허용한 단순안 — 06/08/11 병합 경로에서 image_agent가
`"데이터상태": "visual_only"`를 직접 넣는다(주석으로 방식 명시 — 명세 요구).
organizer `_set_data_status`는 상향만 허용하므로 reported로 강등되지 않음을
확인하고, 세 경로 각각 단위 테스트(이미지 병합 행이 최종 organize 후
visual_only 유지).

## F6. [중요] 매칭 정규화가 `_dedup_key_text` 미재사용 — 가운뎃점 누락

**증상**: `utils/reference_data.py:81-86` `normalise_match_text`는 자체
구현이며 `_dedup_key_text`(organizer_agent.py:273-278)의 가운뎃점 통일
(ㆍ/･/ᆞ/・ → ·)이 없다. NFKC는 이를 커버하지 않으므로 흔한 표기 변형에서
부록4 완전일치를 놓친다. 명세 4-B는 "_dedup_key_text 재사용"을 명시했다.

**수정**: 가운뎃점 통일을 포함시킨다. 순환 import 때문에 organizer의 함수를
직접 import하기 어려우면, 가운뎃점 치환 로직을 reference_data(또는 공용 utils)로
옮기고 organizer가 그것을 재사용하는 방향으로 단일화한다(두 벌 유지 금지).
테스트: "신재생에너지ㆍ보급" vs "신재생에너지·보급" 완전일치 high.

## F7. [중요] WP7 7-C가 무조건 실행 — no-op 수용 기준 위반

**증상**: `organizer_agent.py:1207`에서 관리번호 일관성 검증
(`_append_project_id_consistency_issues`)이 기존계획 장 탐지 여부와 무관하게
항상 실행된다. 명세 WP7 수용 기준 "탐지 패턴 0건 문서에서 v4.1과 완전 동일
출력"을 위반(리포트 항목이 새로 생김).

**수정**: 7-C 검증을 `prior_plan_pages`가 비어 있지 않을 때만 실행하도록
게이팅한다(7-B 태깅과 동일 조건). **no-op 회귀 테스트를 실질화**: 탐지 0건
픽스처로 organize 전체 출력(모든 시트 + 검증리포트)이 게이팅 전 v4.1 경로와
동일함을 dict 비교로 assert.

## F8. [중요] `PRIOR_PLAN_HEADING_PATTERNS` env 확장이 고장

**증상**: `_env_list`(config.py:53-57)는 콤마 분리인데 기본 패턴이 `.{0,6}`
수량자를 포함(config.py:330-338) — env로 같은 부류 패턴을 넣으면 `{0` / `6}`
두 조각으로 갈라져 침묵 오동작(Python re는 미완성 `{0`을 리터럴 취급).

**수정**: 이 키만 구분자를 정규식과 충돌하지 않는 것으로 바꾼다
(권장: `;;` 구분 전용 파서 또는 JSON 배열 파싱 — 주석에 형식 명시).
기본값(env 미설정) 동작 불변. 단위 테스트: `{0,6}` 포함 패턴을 env로 주입해
온전히 파싱됨을 assert.

## F9. [중요] README 미갱신 — 폐루프 "도입 예정" 오정보

**증상**: README.md:88이 "시트 폐루프…는 v5에서 도입 예정입니다"로 남아 있고,
`--sheet-closed-loop` 플래그·명세 8-C가 요구한 알려진 한계(이미지 분석·빈칸
보완이 폐루프 이후 행을 추가해 검수 기준본과 최종본이 다를 수 있음) 문서화가
없다. v5의 나머지 변경(구조적 주입, 타깃 검수, 참조 사전, 데이터상태, 코드북,
장 문맥 프로비넌스)도 README에 반영되지 않았다.

**수정**: README의 v5 변경 요약 섹션을 추가(기존 "v4/v4.1에서 달라진 핵심"
패턴을 따름 — 핵심만 자세하고 간결하게), 88행 문구를 "v5에서 도입 완료
(기본 비활성, `--sheet-closed-loop`)"로 교정, CLI 옵션 표에
`--sheet-closed-loop` 행 추가, 폐루프 알려진 한계 1문단. 이전 버전 기록
보존 원칙(맨 아래 `<details>` 토글) 유지.

## F10. [중요] 폐루프 quota 폴백이 완료 시트 데이터를 유실하는 구조

**증상**: `agents/supervisor.py:348-351`의
`except LLMQuotaExceededError: extractor = ExtractorAgent(); raw_data =
extractor.partial_results()`는 **새 인스턴스의 빈 결과**를 쓴다.
현재는 도달 불가능한 경로임이 확인됐지만(지자체명 추출은 quota를 삼키고,
extract_sheet_pages는 parallel_map_collect가 항목별 실패로 수집, 검수 quota는
루프 안에서 처리), 향후 변경으로 도달하면 완료 시트의 추출·후보·병합로그가
조용히 전량 유실된다.

**수정**: `_run_sheet_closed_loop`이 사용하는 extractor/누적 상태를 예외
경로에서도 접근 가능하게 재구조화한다(예: extractor를 try 밖에서 생성해
공유하거나, 누적 컨테이너를 인자로 전달). 최소한의 변경으로 "폴백 시 그
시점까지의 부분 상태가 보존됨"을 보장하고 회귀 테스트 1개(강제 예외 주입 →
완료 시트 데이터 보존 assert)를 남긴다.

## F11. [중요] STAGE_PROVIDER_REVIEW + HYBRID_REVIEW_PROVIDER 조합 오동작

**증상**: 가용성 검사(hybrid_review_agent.py:607-610)는 stage-aware지만,
실제 호출부(:686, :754, :1150, :1190)는 `HYBRID_REVIEW_PROVIDER` 기준으로
`_effective_backend_model`/`_temporary_backend`를 구성한다. 두 값이 서로
다르면(예: HYBRID_REVIEW_PROVIDER=claude + STAGE_PROVIDER_REVIEW=codex)
codex가 claude용 모델명으로 호출되는 무증상 오동작.

**수정**: 우선순위를 **STAGE_PROVIDER_REVIEW > HYBRID_REVIEW_PROVIDER**로
정의하고, 호출부의 provider·model 결정이 실제 라우팅되는 provider를 따르도록
일원화한다(`_review_stage_provider` 헬퍼를 호출부에서도 사용). config 주석에
우선순위 문서화. 테스트: 두 값이 다를 때 호출 provider와 모델이 일관됨을 mock으로 assert.

---

## S. 사소 일괄 (저위험만 수정, 판단 후 스킵 가능 — 스킵 시 사유를 커밋 메시지나 PR 노트에 남길 것)

1. **멱등성 테스트 확장**: cleaner 멱등성 테스트를 16개 추출 시트 전부로
   파라미터라이즈(tests/test_sheet_closed_loop.py:85-98 현재 1개 시트).
   비멱등 cleaner 발견 시 수정 — 명세 8-B 요구.
2. **8-B 관계 주석**: extract_sheet_pages docstring에 "기존 extract()는 이
   메서드를 재사용하지 않고 분리 유지" 명시(명세가 요구한 선택지 명시).
3. **주입 프롬프트의 앵커 HTML 주석 제거**: guideline_parser에서 섹션 텍스트
   조립 시 `<!-- sheets: ... -->` 라인 제거(시트당 30~100자 노이즈).
4. **부록4 파싱 실패 stderr 보고**: build_reference_data.py가 실패 행을
   조용히 스킵하지 않고 stderr로 보고(명세 4-A 요구).
5. **`_env_list` 행번호 어긋남**: F3에 포함됨(부문 불일치 리포트 인덱스).
6. **registry raw_ids 데드 코드**(organizer_agent.py:926,928): 08 시트 내부
   표기 흡수 보고를 구현하거나 수집 코드를 제거.
7. **06/08/11 외 사소 항목은 이번 턴 범위 밖** — 최장값 vs 최빈값(명세 자체
   모호), 장 경계 다중 구간, openai stage 미지원 정리는 수정하지 말고 그대로 둔다.

---

## 최종 인수 절차

1. 8개 커밋 각각에서 `python -m pytest tests/ -q` 전체 통과(worktree로 확인).
2. HEAD에서 전체 테스트 + 신규 회귀 테스트 통과.
3. `git grep`: F1 내부 필드가 EXCEL_HEADERS와 무충돌, F4 dead flag 해소.
4. 플래그 미설정 실행 스키마 불변:
   `DATA_STATUS_ENABLED=0 PROVENANCE_ENABLED=0 CODEBOOK_SHEET_ENABLED=0` 조합 확인.
5. 절대 제약 재확인: opt-in 기본값 4종 False, 라우팅 로직 diff 없음.
6. push 금지 — 완료 보고 후 사용자 검토 대기.
