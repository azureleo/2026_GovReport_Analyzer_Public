# v7-3 실측 — 06 프롬프트 개정 + codex luna 재추출 (2026-07-16)

> 지시서: `docs/specs/v7-3_06프롬프트_luna재추출_지시서.md` +
> 검증수정 `docs/specs/v7-3_검증수정_지시서.md`(V1·V2).
> 구현: Opus(P1~P4 3커밋 + V1·V2 2커밋). 검증·실측: Claude.
> 판정 **합격**, HEAD a8006bf(테스트 336, origin 푸시).
> **채점 축 주의: 백엔드(flash-lite→luna)+프롬프트 동시 변경** — 06/05 외
> 시트의 변화는 백엔드 등급 효과, 06은 프롬프트 효과와 혼합. 하한선
> 축(flash-lite, v7-2 채점기)과 별도 축이다.

## 실행 구성

- 텍스트 `--agent codex --agent-model gpt-5.6-luna`, vision codex 기본
  (`CODEX_VISION_MODEL=gpt-5.6-luna`). luna 채택 근거: vision 통제 벤치
  1위(0.963·환각 0)+텍스트 실측 존재(terra는 통제 실측 없음, 사용자 위임).
- 지자체별 신규 캐시 `.cache/llm_responses_luna73_{서울,강원,경기}`,
  `LOCAL_AGENT_TIMEOUT=900`. 전 지자체 호출 실패 0(서울 415·강원 264·
  경기 416), 원장 383/245/393 전수 성공, quota 대기 0.
- 중단 내성 실증: 강원 런이 2회 외부 중단됐으나 캐시 보충런으로 손실 0
  재개(155→156→완주).

## 서울 채점 (채점기 v7-2 축, 골든 3,522행)

| 지표 | 하한선(flash-lite, v7-2) | **v7-3 luna** |
|---|---|---|
| 텍스트 유래(의미완화) 리콜 | 0.6101 | **0.8447** |
| 값일치율(텍스트 유래) | 0.8864 | **0.9278** |
| 시각 유래(문자유사) 리콜 | 0.1688 | **0.2370** |
| 시각 값일치율 | 0.7576 | **0.8125** |
| 06 리콜 / 값일치율 | 0.4082 / 0.5000 | **0.4898 / 0.8675** |

- "리콜은 백엔드 등급이 지배" 명제가 풀 파이프라인에서 재확인됨(팀원
  GPT-5.5 실측 0.87/0.90은 판정골든 1,760행 축 — 직접 비교 불가하나
  등급 일치).
- **v7-1 잔여 수송 혼합 2쌍(2213↔200500, 3285↔336400) 소멸** — P1
  enum 개정의 직접 효과. 진짜 총괄 행(19778/29667, p.29·147) 보존.
- luna는 값을 천톤 원문 그대로 유지(86.2 등) — 단위 계약 정리에 유리.
- 06 잔여 엄격 불일치: 총괄 지역전체 2030/2033 쌍은 기준연도 축 상이
  (2005/52342 vs 2018/49445)의 채점 아티팩트(채점기 06 키 기준연도
  미포함 — 사람 결정 대기), 세부사업 키 충돌(06 사업명 컬럼 부재 —
  사람 결정), 농축산 감축률 반올림(2→2) 아티팩트.

## V1·V2 사건 — 06 시각 차단 정책의 잔존 홀 2개 발견·해소

재추출(vision 캐시 무효)이 luna의 새 응답 구조로 잠자던 경로를 깨웠다:

1. **V1(자동 경로)**: image_agent "종류별 분기"(kind=목표→existing_targets
   직접 append)가 M2-4.7의 target_sheet 차단을 우회 — p.200 경로표 60행이
   06에 visual_only로 유입(값 역할 오배치). v6.3 S6 "경로 오지정"과 동일
   계열. 수정: 종류 추론을 병합 판정 전으로 승격해 can_merge 차단+사유
   기록, 레거시 append 제거.
2. **V2(라벨 경로)**: V1이 차단한 관찰값이 반영여부=검토가 되자 라벨
   병합 경로가 재평가, 흡수 2행이 03에 감축량→배출량 오배치로 편입.
   수정: 종류 추론을 관찰값 명시 필드(`종류추론`)로 기록(M2-4.2 "마커
   별도 필드" 권고 이행)+라벨 경로 G4 차단.
- 재실측(캐시 414/1): **06 visual_only 0, 03 visual 87 복귀, 헤드라인
  무하락**, 차단 60건 사유 정합. 교훈: 시트 차단 정책은 "모든 병합
  경로"에 대해 명시적으로 닫아야 하며, 캐시 무효화 라운드는 사문 경로의
  재활성 위험을 게이트로 감시해야 한다.

## 3지자체 분포 (게이트: 값 오병합 0·분포 상식성)

| 지자체 | 06 visual_only | 06 목표수준 분포(총괄/부문/세부부문/세부사업/연차경로) | 재태깅 |
|---|---:|---|---:|
| 서울 | **0** | 12 / 22 / – / 89 / 81 | 347 |
| 강원 | **0** | 8 / 18 / 15 / 13 / 87 | 150 |
| 경기 | **0** | 6 / 18 / 1 / 36 / 78 | 398 |

강원·경기 총괄 행 표본 전수: 목표 장 유래의 완전한 목표 구조(기준배출량·
전망·목표배출량·감축률) 보존 확인(강원 p.45~46·117~118·147, 경기
p.133~134 관리권한 2030 40%/2033 45%). 산출물
`output/{서울,강원,경기}_v73_luna.xlsx` + `golden_score_v73f_서울.md`.

## 잔여 관찰 (다음 라운드 재료)

1. 요약 장 총괄 문장의 세부사업 과분류(서울 p.62~65) — 진짜 총괄 보존으로
   채점 무해, 다음 텍스트 프롬프트 라운드에서 지침 보강.
2. 총괄 목표범위 생략(P1 "불확실하면 생략"의 트레이드오프)으로 골든 9가
   p.187 행(2005 축)과 매칭 — 채점기 06 키 기준연도 편입과 같은 묶음
   (사람 결정).
3. 06 세부사업 키 충돌(사업명 컬럼 부재) — 골든 계약 변경(사람 결정).
4. 경기 총괄 행의 단위 혼재(727.6=백만톤 축) — 단위 계약 라운드 재료.
5. 텍스트 기본 백엔드의 luna 전환 여부 — 비용(구독 quota) 대 성능
   (+23pp 리콜), 사람 판단.

## 기준선 (luna 축, 채점기 v7-2 — v7-4로 대체됨, 아래 절 참조)

서울 텍스트 유래(의미완화) **0.8447/0.9278** · 시각 유래(문자유사)
**0.2370**(값일치 0.8125) · 06 리콜 0.4898·값일치 0.8675 · 06 시각
오염 0(3지자체) · 하한선 축(flash-lite 0.6101/0.8864)은 별도 유지.

## v7-4 — 골든 계약 묶음 (2026-07-16 추가, 합격)

지시서 `docs/specs/v7-4_골든계약_채점기기준연도_지시서.md`. 사용자가
"가이드라인 기반 + 명시 없으면 Claude 권고" 위임 → 4건 판정: ⓐ 채점기
06 일차키 기준연도 편입(§5.2 base_year·32행 축 분리 요구·§2.6.2 산식
근거) ⓑ 골든 행9 축 교정(산식 위반 40%≠30 → 기준연도 2005·감축률 40,
원문 p.147) ⓒ 골든 42·44행 채점제외(06 계약에 사업명 컬럼 없음)
ⓓ 단위 계약 보류(§2.6.1 원문 표기 허용).

**지시서 2차 정정 사건**: Opus의 충돌 보고 규약이 두 번 연속 지시서
결함을 구현 전에 잡음 — ① 빈 키 성분 규약 오전제(실제 `키()`는 빈
성분 시 엄격 키 없음) ② v7-2 가드가 목표수준 발산에만 적용되는 사각
(기준연도만 다른 쌍의 완화 무검증 재결합). 각각 순수 삽입+기존 규약
준수(G1), 가드의 기준연도 발산 확장(G2)으로 정정. 부수 발견: 행9의
2005 교정으로 총괄 지역전체 쌍은 같은 기준연도의 **인벤토리 축 상이**
(서울시 인벤토리 49,445 vs 연료공급량 52,342)가 되어 D1로 분리되지
않음 — 06에 인벤토리 구분 필드가 없는 계약 한계, 발주 문의 후보.

**검증(합격)**: Opus 1커밋(b4173d4) diff 문면 정합, 클린 테스트 342
독립 재현, 3축 재채점(v73f 구골든·구채점기 / A 신골든·구채점기 / B
신골든·신채점기) 전수 열람 — 값 불일치·미매칭 집합 A=B 완전 동일(신규
가짜 쌍 0), 엄격→완화 강등 1건은 값 동일 쌍의 티어 이동(허용 유형),
06 외 전 시트 동일, 정당 매칭 소실 0. ff 병합(HEAD b4173d4, 테스트
342, origin 푸시).

### 기준선 (luna 축, 채점기 v7-4 — v7-5로 대체됨, 아래 절 참조)

서울(골든 3,520행 채점: 행42·44 제외 반영) 텍스트 유래(의미완화)
**0.8447/0.9280** · 시각 유래(문자유사) **0.2378**(값일치 0.8125) ·
06 리콜 0.4894·**값일치 0.8795** · 잔존 06 불일치 = 인벤토리 축
2×4쌍(계약 한계·발주 문의 후보)+농축산 반올림 2쌍(organizer 재계산
관찰)뿐. 리포트 `output/golden_score_v74_서울.md`.

## v7-5 — 서술형 시트 의미완화 확장 (2026-07-17 추가, 합격·진단 교정)

지시서 `docs/specs/v7-5_서술형_의미완화확장_지시서.md`. Opus 1커밋
(4725936) 독립 검증 후 ff 병합(HEAD 4725936, 테스트 363). 서술형
지시서-코드 충돌 1건(토큰 필드 헤더명 — 내용→항목값, 세부전략→설명)은
Opus 보고로 구현 전 교정.

- **구현 인수(합격)**: diff 문면 정합(시트별 토큰·전제, 문자유사 02
  전용 게이트, 비수치시트 의미완화가 수치시트 집계에 섞이지 않게 분리
  — 필수 교정으로 인정), 클린 테스트 363, 신규 의미완화 쌍 2건 전수
  열람 **오결합 0**(01 "비전"↔"장기 비전" 동일 문구, 13 부서명 접두
  차), 02 기존 쌍·타 시트 완전 불변.
- **진단 교정(정직 기록)**: §0의 "키 발산 지배(01 77%·07 73%)" 진단은
  **과대**였다 — 커버리지 지표가 이미 매칭된 행을 포함했고, "국제협력
  부문 자문회의"↔"전문가 자문회의", "녹색산업 육성"↔"녹색성장 촉진"
  같은 **서로 다른 사실**을 '내용 존재'로 오분류했다. 규칙 후보
  시뮬레이션(키 자카드 0.6 / 키 커버리지 0.8 / 전체 커버리지 0.6)
  결과 느슨한 규칙은 회복 대신 오결합을 생성 — **티어 추가 완화 기각**.
- **재진단**: 서술형 잔여 갭의 본류는 표기 발산이 아니라 골든과 출력의
  **세분 수준(granularity)·명명 발산**이다(같은 주제를 다른 단위로
  기록). 처방은 채점기가 아니라 골든 명명 규약 정비(사람 작업) 또는
  발주 협의 영역이며, 진짜 추출 부재(01 8·07 19·13 7행 — 주로 구 계획
  절)는 라우팅/프롬프트 라운드 후보로 유지.

### 새 기준선 (luna 축, 채점기 v7-5)

서울 텍스트 유래(의미완화) **0.8453/0.9280**(의미완화 74건: 02 72+
01 1+13 1) · 시각 유래(문자유사) 0.2378(값일치 0.8125) · 06 리콜
0.4894·값일치 0.8795 · 01/07/13 리콜 0.2857/0.2394/0.6875(엄격·완화
불변). 리포트 `output/golden_score_v75_서울.md`.
