# M2-1 시각 요소 리콜 기준선 (2026-07-06, 확정 v3 → 최종 v4)

> **v4 최종치**: vision_유실 8건 재검토 결과 7건이 비대상(국가·전국 참고 4, 지도 1,
> 방법론 도식 2)으로 판명되어 재분류 — **codex 최종 리콜 0.988 (81/82)**, 진짜 유실은
> E078 비전 체계도 1건. gemini flash-lite는 동일 기준 0.707(vision_유실 24) — 상세는
> `docs/audit/M1_기준선_잠정_20260706.md`.

> 입력: `data/golden/서울_시각요소_인벤토리_v1.xlsx`(176요소 = 캡션 505건 → 텍스트표 제외
> → Claude 페이지 판독 96건 → **사람 원문 확인 6건 + E011·E179 재분류 반영**)
> vs `output/서울_결과_codex.xlsx`. 리포트 원본: `output/visual_inventory_audit_20260706_171739.md`.
> v1 대비 변경: "이미지표" 5건은 텍스트표 판명 제외, E148 도식 재분류(사람),
> E011 런던 로드맵은 국제 참고사례로 기대추출 제외(필터 정상 동작), E179는 조직도 판명(수치 없음).

## 확정 기준선

- **전체 리콜: 0.900** (기대추출 90개 중 81개가 `16_시각자료목록`에 기록)
- 그래프 **0.906** (77/85) · 이미지(도식) 4/5
- **이미지_미추출 0건, 참고자료_제외 오탐 0건** — 서울 문서에 진짜 "이미지 표"는 없고,
  참고자료 필터가 걸러낸 것은 전부 정당한 제외였다.

## 유실 9건의 단계 분포

| 단계 | 건수 | 다음 행동 |
|---|---:|---|
| vision_유실 | 8 | 이미지 확보·triage 통과 후 vision이 데이터 반환 실패 → **M2-3 벤치마크의 표적 셋** |
| triage_탈락 | 1 | E141(p389) 단 1건 |

## 결론 (교정 전 v1과 달라진 것)

1. **G3(triage 리콜) 기각 유지**: `IMAGE_TRIAGE_MIN_SCORE` 3~6 어디서나 리콜 동일 — 완화 근거 없음.
2. **v1의 "벡터 렌더 임계 완화 검토" 권고 철회**: 근거였던 이미지_미추출 5건이 전부
   텍스트표 오분류였다. `VECTOR_RENDER_MIN_DRAWINGS` 변경 불필요.
3. **유일한 실병목은 vision 판독(8건)** → 다음 개발 트랙은 M2-2(렌더 경로)가 아니라
   **M2-3(vision 모델 벤치마크)**. 유실 8건 페이지: 리포트 상세 표 참조.
4. 과추출: 기대추출=N 요소의 페이지 중 33개가 16시트에 기록됨 — 정밀도 개선 여지(후순위).

## 방법론 교훈 (자동 판별의 계통 오류)

렌더된 페이지 이미지만 보고는 **텍스트표와 이미지표를 구분할 수 없다**(드래그 선택
테스트는 사람/텍스트레이어 검사만 가능). Claude 서브에이전트 판독 96건 중 이
실패 모드에 해당하는 6건 전부가 오판이었고 사람 확인에서 교정됐다. 향후 자동 판별은
"표 본문 텍스트가 PDF 텍스트 레이어에 존재하는가"를 기준으로 할 것(숫자 토큰 수만으로는
정성표를 오판한다). 그 외 유형(그래프·사진·도식) 판독은 이 실패 모드의 영향이 없다.

## 골든셋 트랙 현황 (같은 날)

- `data/golden/서울특별시_골든셋_v1_초벌.xlsx`: 3소스(codex·폐루프·정리본) 교차합의
  1,683행 채점 편입, 값불일치 77건·단독 행은 `골든_채점제외=1` 격리. 하네스 형식 검증 통과.
- 합의 골든의 시각후보 0건(세 소스 모두 텍스트 추출 계열) — 시각 유래 정답은 이 인벤토리의
  vision_유실 8건 판독값을 사람이 확정해 골든에 추가하는 것이 다음 단계.
- gemini 독립 실행본(`output/서울_결과_gemini_v5.xlsx`) 생성 중 — 완료 시 합의 강화 +
  비순환 기준선 채점 예정.

## 남은 사람 작업

1. 골든셋 값불일치 77건 판정(`data/golden/골든셋_사람확인목록.md`).
2. 인벤토리 스팟체크 계속(선택): 이미지표 실패 모드는 이미 전수 교정됐으므로
   그래프/사진 표본 ~10건이면 충분.
3. 참고자료_제외 2건(E011·E179) 재분류 판단.
