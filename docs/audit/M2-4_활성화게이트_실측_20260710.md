# M2-4+5 시각 병합 활성화 게이트 실측 보고 (2026-07-10)

> 대상 커밋: 86daa25 (feat/v6-measurement-baseline HEAD). 명세 §4 "3중 게이트" 중
> 서울 A/B 실측 결과. 판정: **기본 True 전환 불가 — 수정 라운드 필요**
> (`docs/specs/M2-4_검증수정_지시서.md`).

## 실행 조건 (재현 가능)

- 입력: `서울특별시_탄소중립계획.pdf`, 코드 86daa25
- 텍스트 스테이지: gemini-2.5-flash-lite + 캐시 `.cache/llm_responses_gemini_v5` (호출 ~0)
- vision 스테이지: `STAGE_PROVIDER_VISION=gemini STAGE_MODEL_VISION=gemini-2.5-pro` (M2-3 실측 캐시 재사용, 호출 ~0)
- 채점: `scripts/score_against_golden.py` + `data/golden/서울특별시_골든셋_v1_초벌.xlsx`(확장 3,218행)

```bash
LLM_PROVIDER=gemini GEMINI_MODEL=gemini-2.5-flash-lite LLM_CACHE_ENABLED=1 \
LLM_CACHE_DIR=.cache/llm_responses_gemini_v5 \
STAGE_PROVIDER_VISION=gemini STAGE_MODEL_VISION=gemini-2.5-pro \
[VISUAL_MERGE_LABELED_ENABLED=1] python main.py 서울특별시_탄소중립계획.pdf -o ...
```

## 결과 요약

| 런 | 병합 | 생략 | 경고 | 차단 | 텍스트유래 리콜 | 값일치율 | 시각유래 리콜 |
|---|---:|---:|---:|---:|---:|---:|---:|
| A. 플래그 off | — | — | — | — | 0.5818 | 0.8661 | 1/4 |
| B. 플래그 on (원판) | **0** | 0 | 0 | **1,491** | 0.5818 (동일) | 0.8661 (동일) | 1/4 |
| C. 플래그 on + G1 의미론 교정(실험) | 40 | 0 | 0 | 1,451 | 0.5821 (+1 매칭) | 0.8658 (±0) | 1/4 |

- A=B 완전 동일: 게이트가 실데이터에서 한 건도 통과하지 못함.
- 기존 표-고신뢰 자동병합 경로는 A/B 모두 visual_only 299행(02:108, 03:16, 05:108, 06:67)으로 정상 작동 — 게이트와 무관.
- C의 병합 40행은 전부 02_지역여건, 지표범주="시각자료"(기본값), 골든 매칭 기여 +1행.

## 발견 결함 (차단 사유 분석)

1. **G1 의미론 불일치 (치명)** — B의 차단 1,491건 중 1,490건이 G1 포함.
   image_agent 프롬프트(#49)는 축 추정값일 때만 `{"estimated": true}`를 넣도록 지시
   → 라벨 판독은 estimated 키 자체가 없음. G1은 명시적 `estimated=false`를 요구해
   라벨 기반 판독이 전부 차단됨. 기존 자동병합 경로(image_agent #667)는
   `fields.get("estimated") is True`만 차단하는 관례 — G1이 이 관례와 어긋남.
2. **키 필드 기본값이 교차검증(G5)을 우회 (치명)** — C에서 생략·경고 0건.
   `_visual_candidate_row`가 판독필드에 없는 키 필드를 기본값(02 지표범주="시각자료",
   03 배출유형="직접배출", 05 시나리오="BAU", 11 재원구분="합계" 등)으로 채워
   후보 키가 텍스트 행 키와 절대 겹치지 않음 → M2-5 교차검증이 무력화되고
   저품질 행이 "텍스트 행 없음"으로 병합됨(40건 중 골든 매칭 1건).
3. **골든 시각 정답 부족 (판정 데이터 한계)** — 골든의 그래프 유래 4행은 전부
   "합의승격(GPT5.5)·출처유형_확인필요" 상태이고 출처페이지 없음(2행은 외부 통계
   출처로 문서 시각자료인지 불명). "시각 유래 리콜 상승" 판정은 골든 시각 정답
   보강 후에만 유의미.

G1 교정 후 잔여 차단 분포(C): G3 계열 1,211(키·값 누락), G2 계열 692(low 신뢰도),
G1 잔여 1,147(판독필드 자체 부재·추정값) — 상당수는 정당한 보수적 차단으로 판단.

## 판정

- `VISUAL_MERGE_LABELED_ENABLED` 기본 False 유지. 기본 True 전환 **보류**.
- 수정 라운드(V1~V3, `docs/specs/M2-4_검증수정_지시서.md`) 후 서울 재실측 +
  강원·경기 분포 확인(명세 §4의 나머지 두 게이트)을 다시 돌린다.
- 골든 시각 정답 보강(인벤토리 확정분 기반)은 별도 데이터 트랙.
