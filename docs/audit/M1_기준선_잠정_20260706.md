# M1 잠정 기준선 — 서울 gemini 독립 실행 vs 골든셋 (2026-07-06)

> 출력: `output/서울_결과_gemini_v5.xlsx` (gemini-2.5-flash-lite, 보충런 4회 누적,
> 503 폭풍으로 배치 14개 실패 잔존 — 캐시 `.cache/llm_responses_gemini_v5` 재실행 시 추가 회복 가능).
> 골든: `data/golden/서울특별시_골든셋_v1_초벌.xlsx` (합의 1,683 + 판정 편입 71 = 채점 1,754행.
> 전량 텍스트 유래 — 시각 유래 정답은 인벤토리 트랙 담당).
> 채점: `scripts/score_against_golden.py` (리포트 `output/golden_score_gemini기준선_최종잠정.*`).
> **"잠정"인 이유**: 골든이 부분(합의행만)이고, 출력에 503 구멍 14배치가 남았으며,
> 채점기 정규화 갭(아래) 보정 전이다. 그래도 이 숫자가 프로젝트 최초의 측정 기준선이다.

## 최종 확정치 (보충런 5회 · 골든 1,760행 · 교정 인벤토리 기준, 2026-07-06 23:57)

- **텍스트: 종합 리콜 0.674 (1,186/1,760) · 매칭 행 값일치율 0.829**
- **시각: codex 0.988 (81/82, 유실은 E078 1건) vs gemini flash-lite 0.707 (vision_유실 24)**
  — 503 재시도 소진 후에도 남은 격차로, vision 판독력의 실제 차이. M2-3 교차 프로바이더
  벤치마크(claude·gpt 포함)의 핵심 근거. 잔존 503 배치 8개(추가 보충런으로 회복 가능).
- 09 보류 3건은 지표 분리 행 추가로 해소(값불일치 잔여 0건).

## 텍스트 추출 기준선 (run4 시점 참고치)

- **종합 리콜 0.666** (1,169/1,754) · **매칭 행 값일치율 0.833**
- 출처페이지 교집합 0.92 — 값을 맞는 위치에서 읽고 있음

| 시트 | 리콜 | 값일치율 | 비고 |
|---|---:|---:|---|
| 05_배출전망 | 1.00 | 0.85 | |
| 06_감축목표 | 1.00 | 0.11 | 값일치 저조 — 실불일치 후보(감축률 30vs40) + 스케일 확인 필요 |
| 08_감축사업목록 | 0.94 | - | 텍스트성 |
| 10_정량감축량 | 0.87 | 0.80 | |
| 12_대응기반강화 | 0.86 | - | |
| 04_배출현황_관리권한 | 0.83 | 0.49 | 값일치 상당수가 채점기 아티팩트(아래 정규화 갭) |
| 09_연차별이행계획 | 0.82 | 0.95 | |
| 02_지역여건 | 0.46 | 0.93 | |
| 07_비전전략 | 0.43 | - | |
| 03_배출현황_지역 | 0.38 | 1.00 | |
| 11_재정투자계획 | 0.33 | 0.96 | 503 구멍 + 대형 시트 |
| 01_계획개요 | 0.27 | - | 자유 텍스트 키 한계 |
| 14_점검실적 | 0.23 | 1.00 | |
| 15_변경과제_조치 | 0.00 | - | 골든 3행, 출력 0행 |

- 정밀도 열은 이 단계에서 **무의미**(골든이 부분이라 골든 밖 출력 행 ≠ 오답).

## 시각 추출 기준선 (인벤토리 176요소, 기대추출 90)

| 백엔드(vision) | 리콜 | vision_유실 | 비고 |
|---|---:|---:|---|
| codex | **0.900** | 8 | 이미지 확보·triage는 완벽(유실 0/1) |
| gemini flash-lite | 0.700 | 26 | 503 보정 후에도 codex 대비 18건 더 유실 |

- **triage·이미지 추출은 백엔드 무관 건강** — 유일 변수는 vision 판독력.
- M2-3(교차 프로바이더 벤치마크: gemini Pro/Flash + **claude vision + gpt vision**,
  `STAGE_PROVIDER/MODEL_VISION` env 교체만)의 우선순위 근거 확보. 발주자 의도
  "가장 성능 좋은 수단"에 따라 vision 스테이지 기본값을 측정으로 고른다.

## 채점기 수정 필요(다음 소수정 라운드 — 이 숫자들의 정밀화)

1. `직간접구분` 값 정규화(direct↔직접 등 영문/국문) — 04 엄격키 붕괴로 완화키 오결합 다발.
2. 04 완화키(관리부문+연도)가 과소식별 — 세부부문 또는 직간접구분 유지 검토.
3. 값일치율 분모 정의(한쪽만 빈칸 분리) — M1-2 검증 LOW.
4. (파이프라인) gemini SDK 클라이언트 타임아웃 부재 — 실측 행(hang) 1회.
5. (셋업) requirements.txt에 google-genai·google-api-core 부재 + 오류 메시지 오안내.

## 다음 단계

- 골든 확장: 시각 유래 정답 행 추가(인벤토리 vision_유실 8건의 사람/Claude 판독),
  09 보류 3건 성과지표 분리, 단독(T3) 행의 표본 검증 편입.
- 남은 503 구멍: 오프피크에 같은 캐시로 보충런 1회(누적 62→14, 수 분·수 센트).
- 06 값 불일치(감축률 30vs40 등) 원문 확인 — 진짜 추출 오류 후보.
