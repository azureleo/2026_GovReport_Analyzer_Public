# M2-4.2 시각 병합 경로 수정 — 구현 검증·실측 (2026-07-12)

## 검증 대상

Opus 구현 `feat/m2-4-2-merge-path` 3커밋(906d577·00a5a9a·514dae4, 별도 worktree
GovReport_m242 — 오염 없음). 검증자는 별도 임시 worktree에서 독립 재실행.

## 구현 정합 (diff 전수 검수)

- **S1**: legacy map `"summary"→"vision_strategy"` 제거 + 지역여건 키워드 폴백
  (명세 목록 그대로, 예산 규칙 뒤·전망 규칙 앞 삽입) + `; 대상시트 재추론(summary)`
  근거 부기 — 정합.
- **S2**: `IMAGE_CHART_MERGE_YEARS_BY_SHEET`(02·03 = 1990~2050) + 대상 시트 기준
  연도 검사 전환, 신뢰도 강등 예외 목록 불변 — 정합.
- **S3**: `_infer_indicator_category` — 전 범주 검사 후 유일 매칭만 채택(동률 None),
  regional_conditions 지표범주 공란만 보강, 타 시트·타 필드 미적용 — 정합.
  게이트 판정 로직·estimated 관례·프롬프트·기본 False 전부 불변 확인.
- **S4**: 신규 테스트 2파일(명세 4항목 커버). **커밋별 클린 worktree 테스트
  225→226→226 전부 통과**(bisect 안전).
- 기록(차단 아님): S1 재추론 마커가 근거의 fields JSON 뒤에 `"; ..."`로 붙어,
  차단사유가 없는(=반영) 관찰값에서는 `_visual_fields`의 JSON 파싱을 깨뜨릴 수
  있으나, 라벨 경로는 "검토" 관찰값만 처리하고 검토 행은 항상 사유가 있어
  `" | "` 구분자가 보존됨 — 기능 무해, 후속 라운드에서 마커를 별도 필드로 권장.

## 실측 환경 (캐시, 비용 0)

텍스트 gemini-2.5-flash-lite + vision codex gpt-5.6-luna, 캐시
`.cache/llm_responses_gemini_v5`, **.venv 파이썬 필수**(anaconda 파이썬은 PyMuPDF
동판이라도 페이지 텍스트가 미세하게 달라 캐시 키가 깨짐 — 재현 시 주의).
무효 API 키로 실행해 실호출 원천 차단: **캐시 히트 415/416, 유일한 미스 1건은
최종 품질 검수 호출**(런 결과가 프롬프트에 포함되는 비데이터 호출) — 명세의
"캐시 전량 히트" 인수 조건 충족. 추출 원장 383/383 성공.

## §5 게이트 실측 결과 (서울)

| 지표 | v6.1 off | v6.2 off | v6.1 on | v6.2 on |
|---|---|---|---|---|
| 텍스트 유래 리콜(의미완화 포함) | 0.6042 | 0.6049 | 0.6045 | **0.6170 (+1.25pp)** |
| 값일치율(텍스트 유래) | 0.8697 | 0.8695 | 0.8694 | **0.8731 (+0.37pp)** |
| 시각 유래 리콜(의미완화 포함) | 0.013 | 0.013 | 0.0195 | **0.039 (12/308)** |
| 의미완화 매칭 | 36 | 36 | 38 | 44 |
| 게이트: 병합/생략/경고/차단 | – | – | 14/5/22/1,098 | **108/9/36/929** |

- **게이트 1 (flag-off 무하락): 조건부 통과.** 리콜 +0.07pp. 값일치율은
  0.8697→0.8695(-0.02pp) — 신규 값불일치는 단 1쌍으로, S2가 살린 03 폐기물
  2005 세부부문 시각 행(1,944)을 채점기 완화키(세부부문 미포함)가 골든
  총계(6,900)에 오결합한 **채점 아티팩트**(기지 완화키 과소식별 계열). 추출
  오류 아님. 신규 자동병합 visual_only +73행(02 +34, 03 +39 — S1 재라우팅·S2
  과거연도 표형 행) 보고.
- **게이트 2 (시각 유래 리콜 ≥0.15): 미달(0.039).** 원인 분석 아래.
- **게이트 3 (강원·경기 분포): 실측 불가.** 두 지자체 vision 캐시가
  M2-4.1 스키마 확장 **이전 프롬프트** 산물이라 키 전량 미스 → 관찰값 0 →
  게이트 무발화. 강원·경기 vision 재추출(quota, 사용자 승인 대기 항목 ⓑ)과
  결합해서만 측정 가능.

## 게이트 2 미달 원인 (실측 해부)

병합 108행(02:104, 03:4)에 시각 골든 값(연도+값 0.5%)이 존재하는 행은
**40/308** — 게이트를 전부 열어도 현 판독 필드로는 리콜 상한이 ~0.15 언저리이고,
그 40행마저 명명 발산으로 매칭이 깨진다:

1. **시리즈 키 붕괴(경고 36의 정체)**: '건축물 연차별 현황' 2020 한 키에
   연차구간 값 6개 — 병합 키(지표범주+지표명+연도)가 시리즈를 구분 못 해
   첫 행만 병합, 나머지는 V3 경고. 다시리즈 차트(장래인구 연령대, 건축물
   연차 등)에서 병합 가능분의 대부분을 잃는 구조. 골든과의 매칭도 같은 이유로
   실패(병합행 '연령별 인구' vs 골든 '장래인구추계 0~14세 인구', 자카드 0.25).
2. **05·06 병합 0**: emissions_forecast는 시나리오, reduction_targets는
   목표수준·목표범위가 1차 키인데 vision fields가 안 줌 — 프롬프트 공급 사안.
3. **잔여 차단 929**: G1 판독필드 없음 계열 ~339(fields 미공급 12/31 응답),
   G3 1차 키 누락 계열 ~500(연도 없음 포함), G3 대상 시트 미지원
   ~300(명시적 vision_strategy·mitigation_projects 등 키 정의 없는 시트).

## 실질 개선의 증거 (게이트 숫자 밖)

- 병합 108행이 **텍스트 유래 매칭 +40행**을 만들었다(1,943→1,983): 02 리콜
  0.2674→0.3116(+4.4pp), 03 0.1684→0.1895(+2.1pp), **두 시트 모두 값일치율
  동반 상승**(0.9672→0.9719, 0.6875→0.6944) — 병합 행이 정확한 데이터라는 뜻.
- 신규 의미완화 6쌍 전수 열람: 전부 정당(예: '가정·상업 부문 가스' ↔
  '가정·상업 부문 에너지원별 비중(가스)', 값 동일).
- 생략 9건 전부 G5 교차일치(시각=텍스트 값 동일) — 교차검증 게이트 정상 작동.
- 골든 시각 값 40행이 병합행에 정확값으로 존재 — 외부 정답 대비 검증.

## 게이트 3 실측 (강원·경기, 2026-07-12 재추출 후 추가)

사용자 승인으로 강원·경기 vision 재추출(luna, 강원 12·경기 22호출, 실패·quota 0)
→ flag-on 캐시 재실행(비용 0). 분류 필드 공급률이 서울보다 좋음(fields 보유
관찰값 강원 630/1,084=58%, 경기 1,127/1,660=68%).

| 지자체 | 병합 | 생략 | 경고 | 차단 | 경고/병합 |
|---|---:|---:|---:|---:|---|
| 강원 | 18 | 46 | 17 | 514 | 94% (문면상 기준 초과) |
| 경기 | 158 | 22 | 70 | 1,118 | 44% (통과) |

병합 행 표본 열람(강원 18 전수 + 경기 표본, 원문 텍스트레이어 대조 포함):

- **값 차원 오병합 0건**: 경기 에너지원별 소비는 합계 검산 일치(244+11,500+
  5,323+11,492+1,730+1,211≈31,501), 흡수량 음수 부호 정확, 강원 친환경차
  시계열 단조 증가 정상. 강원 03의 2033·2040 -10,780.68 반복은 복제 오류가
  아니라 **원문 <표 2-52>가 실제로 그렇게 기재**(2020 수준 유지 가정 전망) —
  luna 값은 원문 그대로.
- **배치(자리) 이슈 3종**(값은 정확, 필드/시트 배치 문제 — 다음 소수정 후보):
  ① 흡수 "전망" 표가 05가 아닌 03에 병합(대상시트 배정), ② '관리권한 외
  88.4%' 비율값이 배출량 필드에(단위 % 검사 없음), ③ 경기 가스종
  (이산화탄소·메탄)이 부문 필드에 배치.
- 강원 경고 17건은 전부 '시군별 사업체 수' **시리즈 키 붕괴**(시군별 값들이
  한 키로) — 서울 경고 36과 동일 뿌리로, M2-4.3 필요성의 3지자체 증거.
  오병합이 아니므로 게이트 3은 **조건부 합격**으로 판정(경고 비율 조항의
  취지는 오병합 감지이고, 실체는 전부 M2-4.3 표적).

S2(시트별 연도 범위)의 후속 발견: 03을 1990~2050으로 넓힌 결과 전망성
행이 현황 시트에 들어올 수 있음 — 03 상한을 작성시점 부근으로 줄이는 안을
다음 소수정 후보로 기록.

## M2-4.3 시리즈 지표 가이드라인 정렬 — 검증·실측 (2026-07-12 추가)

Opus 단일 커밋(1b91306, 별도 worktree) 독립 검증: diff 명세 S1 정합
(`_dedup_key_text` 비교·폴백 보존·02 분기 한정), 클린 worktree 테스트 231
통과(+5). 3지자체 캐시 재실행(무효 키, 실호출 0):

| 지자체 | 병합 | 경고(시리즈 붕괴) | 차단 |
|---|---|---|---|
| 서울 | 108→**129** | 36→**14** | 929 불변 |
| 강원 | 18→**35** | 17→**0** (완전 해소) | 514 불변 |
| 경기 | 158→**230** | 70→**13** | 1,118→1,119 |

- **flag-off 서울: 골든 지표 완전 무변**(0.6049/0.8695, 시각 0.013, 완화 36).
- **flag-on 서울**: 텍스트 유래 리콜 0.6170→**0.6207**, 값일치율 0.8731→
  **0.8747**(동반 상승 유지), **시각 유래 리콜 0.039→0.1006(12→31행, 2.6배)**,
  의미완화 44→62. 차단 불변 = 게이트 완화 없이 키 정렬만으로 얻은 개선.
- 병합 증가분 표본 열람(3지자체): 배치가 가이드라인 §5.3 그대로 —
  세부범주='건축물 연차별 현황'/'시군별 사업체 수'(중분류), 지표명='10년
  미만'/'춘천시'(시리즈). 서울 건축물 6구간·강원 시군 18행 전부 회복.
- 잔여 관찰(차단 아님): ① 서울 잔여 경고 14는 시리즈 라벨이 짧아지며
  **다른 차트 간** 동일 키 충돌(예: 에너지 '도시가스' 2005 실적 vs 전망) —
  V3가 보수적으로 첫 행만 유지, 세부범주의 키 편입 검토 후보(기존 "02 키
  과소식별" 항목과 동근). ② 경기 잔여 13은 03 시트 '총괄' 키(범위 밖).
  ③ 경기 일부 지표명 '석탄_최종에너지소비'처럼 밑줄 표기(vision 항목 원문)
  — dedup 정규화가 밑줄을 안 접어 세부범주와 유사 중복(경미·표기 사안).

**M2-4.3 판정: 합격·병합(1b91306).** 시각 유래 리콜 0.1006은 M2-4.2 게이트
기준(0.15)에는 여전히 미달이나, 명세가 예고한 "부분 회복" 범위의 상단 —
잔여 갭은 명명 발산(캡션 유래 골든명 vs vision 차트명)과 05·06 키 미공급
(프롬프트 라운드) 몫. 기본 True 전환 판단 재료: 3지자체에서 값 오병합 0·
품질 동반 상승·경고 대부분 해소로, 전환 반대 근거는 시각 리콜 절대치보다
프롬프트 라운드 이후로 미루는 실익 여부.

## M2-4.4 세부범주 키 편입 + 기본 활성화 — 검증·실측 (2026-07-13 추가)

사용자 권고안 ② 채택에 따른 라운드. Opus 2커밋(5a25d75·a0fad23) 독립 검증:
diff 명세 S1·S2 정합(키 편입 + 와일드카드 + 기본 True, env=0 경로 보존),
커밋별 클린 테스트 234→235 통과(기존 테스트 수정 0). 3지자체 캐시 재실행:

| 지자체 | 병합 | 생략 | 경고 | 차단 |
|---|---|---|---|---|
| 서울 | 129→139 | 10→7 | 14→**9** (한 자릿수 게이트 통과) | 929→927 |
| 강원 | 35 | 46 | 0 | 514 (전부 불변) |
| 경기 | 230→238 | 6 | 13→7 | 1,119→1,117 |

- **off 대조(env=0) 서울: M2-4.3 flag-off와 골든 지표 완전 동일** — 비활성
  경로 보존 증명.
- **기본 실행(=on) 서울**: 텍스트 유래 0.6207/0.8747 무하락(동일), 시각 유래
  리콜 0.1006→**0.1039(32/308)**, 의미완화 63.
- 서울 잔여 경고 9는 세부범주까지 같은 두 관찰값의 실제 값 충돌 — V3의
  정당한 방어. 경기 잔여 7은 03 '총괄' 키(범위 밖 기지 항목).
- 신규 병합 표본(서울 10·경기 8 전수 열람): 배치 정상(2005년 대비 변화율
  연료별, 재생에너지원별 발전량, 폭염·열대야일수 등), 이상치 0.
- **트레이드오프 실측**: 생략 감소분(10→7) 중 최대 3행이 텍스트 행과 같은
  사실의 중복 유입(예: 최종에너지 합계 2021=13,405 — 텍스트·시각 값 동일,
  모순 없음). 세부범주 상이 시 G5 이탈이라는 명세 승인 트레이드오프의
  실현으로, visual_only-텍스트 간 완화 dedup을 소수정 후보로 기록.

**M2-4.4 판정: 합격·병합(a0fad23). 이제 기본 실행이 라벨 병합 포함(on)이며,
이번 기본 실행 채점(서울 텍스트 0.6207/0.8747·시각 0.1039, 3지자체 게이트
분포표)이 이후 회귀 비교의 새 기준선이다.**

## M2-4.5 병합 품질 소수정 — 검증·실측 (2026-07-13 추가)

Opus 3커밋(20766f3·6c951cc·b92f35d, type 프리픽스 준수) 독립 검증: diff
지시서 S1~S5 정합, 커밋별 클린 테스트 235→246→248(기존 테스트 수정은
S5가 의도한 마커 이동에 따른 1건뿐 — JSON 청정성 검증이 오히려 강화됨).
3지자체 캐시 재실행(실호출 0), 대조 기준 = M2-4.4 기준선:

| 지자체 | 병합 | 생략 | 경고 | 차단 | 신규 사유 발화 |
|---|---|---|---|---|---|
| 서울 | 139→138 | 7→9 | 9 | 927→926 | %부적합 62·재지정 13·완화교차 2 |
| 강원 | 35→32 | 46→52 | 0 | 514→511 | %부적합 13·재지정 55 |
| 경기 | 238→234 | 6 | 7→4 | 1,117→1,124 | %부적합 98·재지정 172·가스종 2 |

- **제거된 병합 행이 전부 의도된 표적**(전수 확인): 강원 -3 = 전망
  2033·2040 2행 + 88.4% 1행(03에서 완전 소멸), 경기 -6 = 가스종
  2행(이산화탄소·메탄) + 부문별 비중 %값 4행, 서울 -2 = 값동일 중복
  (최종에너지 합계 2005·2021)의 생략 전환.
- **서울 골든: off·on 모두 M2-4.4와 완전 동일**(0.6049/0.8695·0.013,
  0.6207/0.8747·0.1039) — S4 생략 전환의 리콜 중립이 실측으로 증명.
- 잔여 기록(차단 아님): 경기 03에 부문='총 메탄 배출량' 2행 잔존 —
  S3의 전체일치 규칙상 의도된 통과(부분일치는 과차단 위험). 값·단위는
  원문 정합. 부분일치 확장 여부는 후속 관찰 후 판단.

**M2-4.5 판정: 합격·병합(b92f35d). 새 기준선: 서울 병합 138·생략 9·경고 9·
차단 926, 강원 32/52/0/511, 경기 234/6/4/1,124; 서울 골든 지표는 M2-4.4와
동일(0.6207/0.8747·시각 0.1039).**

## 채점기 문자유사(4차) 매칭 — 검증 (2026-07-13 추가)

명명 라운드의 사전 실측(미매칭 시각 골든 245행 전수 분석): 219행은
판독유실(명명 밖), 진짜 명명 사망은 17행이며 원인은 어휘가 아니라
**토큰화**(붙여쓰기 '등록대수'≠{등록,대수}, 합성명 '공원 수(어린이공원)').
문자 2-gram 자카드 캘리브레이션(17쌍 0.21~0.64 vs 무작위 3천쌍 95백분위
0.06) → 임계 0.30의 4차 티어 명세(`docs/specs/채점기_문자유사매칭_구현명세서.md`).

Opus 2커밋(fe93d44·ed212e3) 독립 검증: diff 명세 정합(전제 재사용·0.30
상수·0.2~0.3 관찰 목록·동률 비매칭·의미완화 소비분 제외), 커밋별 클린
테스트 254→255. 재채점 검증(파이프라인 재실행 0):

- **기존 지표 바이트 불변**: 서울 v45(0.6207/0.8747·의미완화 포함 0.1039·
  의미완화 63)와 v6.1 워크북 모두 완전 동일.
- **시각 유래(문자유사 포함) 0.1039→0.1461** — 명세 예상(0.14~0.15) 적중.
- 문자유사 매칭 51쌍 전수 열람: 오결합 0('비율↔비중', '수↔개수', 공원·
  연료별·건축물 연차 시리즈 등 전부 정당 — 값+연도+단위 전제의 방어 확인).
  관찰(0.2~0.3, 비매칭) 11쌍 별도 기록.
- v6.1 재채점에서도 일관 작동(28쌍, 시각 0.0195→0.0292).

**판정: 합격·병합(ed212e3).** 시각 리콜 여정: 0.013→0.039(M2-4.2)→
0.1006(M2-4.3)→0.1039(M2-4.4)→**0.1461(문자유사 포함, 채점기 정비)**.
명명 부채는 사실상 정리 — 잔여 ~219행은 판독유실·차단으로, 05·06 키 공급
프롬프트 라운드(재추출 수반)가 본류.

## M2-4.6 전망·감축목표 시각 병합 — 검증·재추출 실측 (2026-07-14 추가)

Opus 2커밋(리베이스 후 14d3599·49d9a88) + 검증자 짝 케이스 1커밋(8c8c4f7).
diff 명세 정합(S1 어휘·SSP 규칙, S2 매핑, S3 시나리오 와일드카드), 충돌
테스트는 판정대로 새 의도 단언으로 갱신, 커밋별 클린 테스트 259→262→263.
부수 확인: 부문은 항목·제목 폴백 탓에 G3 누락이 실발생하지 않음(차트 제목이
부문에 들어가는 배치 스멜 — 관찰 기록).

**3지자체 재추출 완료**(luna, 실패 0): 서울 30배치(첫 런 ~25 + 재시작 5),
강원 17, 경기 22. 사고 기록: 동시 실행된 다른 codex 세션과의 경합으로
호출당 소요가 300초 상한을 넘어 타임아웃 루프에 빠짐(~1.5h 정체) →
`LOCAL_AGENT_TIMEOUT=900`으로 재시작해 해소. 완료 배치는 캐시로 보존되어
낭비 0. 다음 대량 재추출 시 900초 상향을 기본 관례로 권장.

### 성과 (게이트·채점)

- forecast·target 라우팅 3지자체 실발화: 05 visual_only 서울 109·강원 33·
  경기 254행, 06 서울 88·강원 65·경기 36행 — 05·06이 최초로 채워짐.
- **서울 05 리콜 0.8611→0.9167**, 02 +1.9pp. **시각 유래(문자유사 포함)
  0.1461→0.1688**, (의미완화 포함) 0.1039→0.1331. 값일치율(텍스트 유래)
  0.8747→0.8758 상승.

### 회귀 발견 — 판정 보류 사유

1. **06 값 품질 회귀(치명)**: 06 값일치율 0.3235, 값불일치 23건. 원인은
   S1~S3가 아니라 **자동병합 경로**: 라벨 경로 병합은 6건뿐인데 06
   visual_only 88행 — 82행이 G5 교차검증·값역할 배치가 없는 자동병합
   경로로 유입. 실물은 같은 값을 천 배 스케일로 두 칼럼에 중복 기입
   (목표감축량 683,000 + 목표배출량 683)하고 감축률에 무관 문자열('200')이
   들어간 망가진 배치. 새 어휘(target)가 사실상 사문이던 자동병합 분기를
   깨운 것. "값 오병합 0" 원칙 위반이므로 **기본 산출물에 실을 수 없음**.
2. 텍스트 유래 리콜 0.6207→0.6173(-0.34pp): 지배 원인은 08 -3.7pp —
   기존에 strategy로 08에 병합되던 감축 카드 차트 일부가 target으로
   이동(라우팅 재배치의 부수 효과). 06이 정상화되면 상쇄 여부 재평가.

### 판정

**S1~S3 구현 인수는 합격이나 병합 보류.** 자동병합 경로의 05·06 유입을
차단하는 후속 수정(M2-4.7: 해당 대상시트는 자동 반영 금지, "검토" 강등으로
라벨 경로 G1~G5만 통과) 후 함께 재실측·병합한다. 재추출 캐시는 신 프롬프트
기준으로 확보 완료 — M2-4.7 재실측은 비용 0.

## M2-4.7·4.8 — 3구성 실측과 경로 확정 (2026-07-14 추가, 최종 합격)

M2-4.7(자동 차단→라벨만) 재실측에서 라벨 경로도 06 품질 미달(값일치
0.21, 값역할 스왑)이고 05 라벨은 리콜 이득 0·경고 94 유발임이 드러나,
3구성 비교로 정책을 확정(사용자 승인): **06=시각 유입 전면 차단,
05=자동병합 복원+라벨 제외(M2-4.6 S3 시나리오 와일드카드 철회)** = M2-4.8.

M2-4.8 검증: diff 지시서 정합, 커밋별 클린 테스트 268→269, 3지자체
캐시 재실측(실호출 0):

| 게이트 | 결과 |
|---|---|
| 06 visual_only | 서울·강원·경기 **전부 0** (오염 제거) |
| 서울 05 | 리콜 **0.9167 회복**, vo 102 (강원 12·경기 75) |
| 경고 | 서울 94→**0** (강원·경기도 0) |
| 시각 유래(문자유사 포함) | **0.1688** (v46 최고치 유지) |
| 텍스트 유래 | 0.6173/0.8744 — 값일치 사실상 동일(-0.03pp), 리콜 -0.34pp는 08 라우팅 재배치(승인된 트레이드오프) |

추가 규명 2건:
1. **06 값일치 0.21의 정체 = 기존 텍스트 추출 이슈**: 시각 오염이 제거되자
   원래 텍스트에 있던 연차별 감축량 행(reported, 예: 건물 2027 762,000 —
   톤 단위)이 dedup에서 되살아나 골든(천톤)과 비교되기 시작한 것. 시각
   병합과 무관 — **소수정 후보: 06 텍스트 행 단위 정규화·연차별 카드와
   목표의 구분**.
2. 08 리콜 -3.7pp 잔존: 사업 카드류 차트가 target으로 분류되는
   over-routing — 프롬프트 미세조정(재추출 수반) 후보로 기록, 06 병합
   차단으로 실해는 데이터 미수록(16시트 보존)뿐.

**판정: 합격. M2-4.6~4.8 일괄 병합(86e69de, 테스트 269).**
**새 기준선(기본 실행, 신 프롬프트 재추출 캐시): 서울 텍스트
0.6173/0.8744·시각 유래(문자유사 포함) 0.1688·05 리콜 0.9167·06 오염 0,
게이트 서울 209/15/0/1,270·강원 49/98/0/830·경기 213/2/0/1,911.**
시각 리콜 여정: 0.013→0.039→0.1006→0.1039→0.1461→**0.1688**.

## 소수정 v6.3 — 구현 검증·실측 (2026-07-14 추가, 수정 라운드 지시)

Opus 8커밋(1823c88~e8df4e7, feat/v6-3-minor-fixes, 별도 worktree, type
프리픽스 준수) 독립 검증: diff 지시서 S1~S9 전 항목 정합, 커밋별 클린
worktree 테스트 269→298 전부 통과(기존 테스트 수정 1건은 S6 의도 충돌
사유 명시). 3지자체 캐시 재실행(.venv, 무효 키) — **캐시 전량 히트**(서울
415/1·강원 263/1·경기 415/1, 미스는 품질검수뿐), 기준선(797c4c1) 병행
재실행과 A/B. 기준선 재현은 완벽: 구 채점기 채점이 M2-4.8 기록
(0.6173/0.8744·0.1688)과 바이트 일치, 게이트 분포 209/15/0/1,270·
49/98/0/830·213/2/0/1,911 그대로.

### v6.3 실측 결과

- **게이트 분포 3지자체 완전 불변**, 변화 시트는 04(S5 표기)·06(S1a 분리)·
  검증리포트뿐(02·03·05·16 바이트 동일). 시각 유래(문자유사 포함) 0.1688
  동일.
- **S5 실발화**: 직간접구분 sink→흡수 서울 41·강원 22행+경기(채점기 양측
  정규화 공유로 골든 매칭 무영향 확인).
- **S1a 실발화**: 06 기준연도 분리 서울 91→100·강원 91→96·경기 100→106행.
  서울 06 리콜 0.5510→0.5714, base가 2005·2018 기준 사실을 한 행에 섞던
  것(감축률 40 오염)이 분리되어 감축률 30 골든 일치 회복.
- S4·S7·S8·S9·S1b: 현 3문서에서 무발화·무회귀(예방 게이트). S8 상한은
  문서메타 계획시작연도(3지자체 모두 2024)+1=2025로 실산출됨 — 사문 아님.

### 발견 1 — S3 회귀 (수정 라운드 표적)

신 채점기로 base 산출물 채점 시 완화 매칭 03 18→10·04 50→18(-40),
텍스트 유래(의미완화) 리콜 0.6173→0.6049(-1.24pp). 제거분 전수 분류:

- 정당 제거 ~23건: '상업'vs'상업/공공'(14,145 vs 3,453), '가정'vs'열-가정',
  '1A 연료연소'vs'연료 공급량 기준' 등 값 상이 오결합 — M2-4.2 아티팩트의
  실체로, 제거 시 값일치율 0.8744→0.8759 상승.
- **부당 제거 ~25건**: '폐기물'vs'폐기물 전체'(4,710=4,710),
  'LULUCF'vs'LULUCF 전체'(-151=-151), '간접배출 계'vs'총배출량'
  (25,399=25,399), '연료 공급량 기준'vs'총배출량(연료 공급량 기준)' —
  값 완전 동일한 표기 발산 정당 매칭. 지시서 S3 규칙(양쪽 채움·상이면
  제외)이 이 계열을 예상 못 한 설계 결함(지시서 귀책, 구현은 문면대로).

### 발견 2 — 06 값일치율 진단 교정

- v6.3 06 값일치율 0.2143→0.1538(-6pp)이나 **하락분은 채점 아티팩트**:
  base에서 기준연도 다른 사실이 우연 병합돼 골든(2018 행에 49,445·29,667
  동봉)과 맞던 일치쌍이, S1a의 정당 분리 후 채점기 06 키(기준연도 미포함)
  가 짝을 못 찾는 것. 추출 자체는 개선(사실 분리·감축률 정합).
- **스케일동치 0쌍 — 지시서의 톤↔천톤 진단 반증**: 06 값불일치 22쌍 전수
  열람 결과 1,000배 관계가 하나도 없음(19,778 vs 9,802,000=495배, 9,877 vs
  86,200=8.7배). 실원인은 **연차별 사업카드 감축량 값의 목표 행 유입**
  (209,000·86,200·24,000류). S2는 무해 사문으로 존치, "연차별 카드 vs 목표
  구분"이 06 본류로 승격(이제 실측 증거 22쌍 확보).
- 골든 06 행 8(기준연도 2018에 49,445·29,667 동봉)의 자체 재판정과 채점기
  06 키 기준연도 편입은 묶어서 별도 판단 대기.

### 발견 3 — S6 경로 오지정

경기 03 '총 메탄 배출량' 1행 잔존. `_gas_sector_key`는 정확히
작동('총 메탄 배출량'→'메탄' 차단 판정)하나, 잔존 행은 **자동병합 경로**
(표-고신뢰) 유입이라 라벨 경로의 게이트가 보지 못함 — 지시서의 경로
오지정(구현 결함 아님).

### 판정

**S1a·S5·S7·S9 합격, S4·S8·S1b·S2 무해(예방/사문), S3 회귀로 병합 보류.**
수정 라운드 `docs/specs/소수정_v6.3_검증수정_지시서.md`(V1 S3 값불일치
조건 강화, V2 S6 자동병합 경로 확장) 후 재실측·병합한다. 재실측은 캐시
전량 히트라 비용 0.

## 판정

- **구현 인수: 합격.** 명세 전 항목 정합, 커밋별 테스트 통과, flag-off 실질
  무회귀(아티팩트 1쌍), flag-on 순개선(+40행, 품질 동반 상승, 오병합 신호 0).
  → feat/v6-measurement-baseline에 병합.
- **`VISUAL_MERGE_LABELED_ENABLED` 기본 True 전환: 계속 보류.** 시각 유래
  리콜 게이트 미달(0.039<0.15) + 강원·경기 게이트 미실측.
- **다음 지렛대(효과 큰 순)**:
  1. 병합 키에 시리즈 한정어 편입(지표세부범주 또는 항목의 시리즈 라벨) —
     경고 36 해소 + 다시리즈 차트 병합 회복. 기존 "02 지표명 키 과소식별"
     항목과 동일 뿌리.
  2. vision 프롬프트에 05 시나리오·06 목표수준/목표범위 공급 요구 + fields
     미공급(12/31) 응답 개선 — **캐시 무효화(재추출 비용) 수반**, 강원·경기
     재추출 승인과 묶어 한 라운드로 처리하는 것이 경제적.
  3. 명명 정합: 38+6쌍 의미완화 재료 기반 지표명 규약(한정어 괄호 표기) 또는
     02 의미완화 임계 재검토(단 오결합 위험과 트레이드오프).
